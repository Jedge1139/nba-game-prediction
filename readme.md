# NBA Game Prediction

Predict NBA game outcomes using historical stats, efficiency metrics, and ensemble machine learning models.

## Project Structure

- `src/main.py`: Full code with feature engineering, Random Forest, Gradient Boosting, and Voting Ensemble.
- `data/`: Place your `games.csv` here.
## Setup

1. Clone the repo:

```bash
git clone https://github.com/YOUR_USERNAME/nba-game-prediction.git
cd nba-game-prediction
